{
  "name": "lunch-photo-push-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:import-schools:local": "node scripts/import-schools.mjs --local",
    "db:import-schools:remote": "node scripts/import-schools.mjs --remote",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260904.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.129.0"
  }
}
